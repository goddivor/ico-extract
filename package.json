{
  "private": true,
  "name": "ico-extract-dev",
  "description": "Dev scripts for ico-extract. The published npm package is generated by wasm-pack into pkg/ (publish from there).",
  "scripts": {
    "build": "wasm-pack build --target nodejs --release --features wasm",
    "test": "cargo test",
    "publish:npm": "npm run build && cd pkg && npm publish"
  }
}
